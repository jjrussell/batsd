- @page_title = 'New Offer'

= render('dashboard/apps/offers_shared/descriptions')
%br

= form_tag(app_offers_path(@app, :app_metadata_id => params[:app_metadata_id], :offer_type => params[:offer_type])) do
  = submit_tag('Create Offer')
