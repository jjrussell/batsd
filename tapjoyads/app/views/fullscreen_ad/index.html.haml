%a.ad{ :href => @offer.get_click_url(@publisher_app, params[:publisher_user_id], params[:udid], params[:source], params[:app_version], @viewed_at, nil, params[:exp]) }
  %p
    Earn #{@currency.get_visual_reward_amount(@offer)} #{@currency.name}. 
  %p
    Just download and run #{@offer.name}.
  %img{ :src => @offer.get_icon_url }
  
- content_for :page_styles do
  :plain
    body {
      margin: 0;
      padding: 0;
    }
  
    .ad {
      display: block;
      width: 280px;
      height: 440px;
      color: black;
      text-decoration: none;
      padding: 20px;
      font-size: 20px;
      text-align: center;
      background: url('http://s3.amazonaws.com/tapjoy/images/brushed-metal-320x480.png');
    }
    
    img {
      border: 0;
      display: block;
      margin: 0 auto;
    }
      