- @page_title = 'This offer is no longer enabled'

#main
  The reward offer for 
  %span.name #{@offer.name} 
  is no longer enabled. You can return to the previous page
  and refresh to reload the latest offer list.
  %br
  %br

  %a{ :href => @offer.get_destination_url(params[:udid], params[:publisher_app_id]) }
    Or, click here to download #{@offer.name} anyways.

-content_for :page_styles do
  :plain
    #main {
      padding-bottom: 15px;
      font-size: 115%;
    }
    .name {
      font-weight: bold;
      color: #008810;
    }
