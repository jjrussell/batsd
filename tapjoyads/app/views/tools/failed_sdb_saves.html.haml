- @page_title = 'Failed Sdb Saves'

%h1 Failed Sdb Saves

Number of domains: #{@failed_sdb_saves.size}

%table#fails.tablesorter
  %thead
    %tr
      %th Domain Name
      %th This Hour (SDB)
      %th Last Hour (SDB)
      %th This Hour (MC)
      %th Last Hour (MC)
  %tbody
    - @failed_sdb_saves.each do |domain_name, counts|
      %tr
        %td{ :class => @bad_web_requests[domain_name] ? 'bad_domain' : '' }= domain_name
        %td= counts[:sdb_this_hour]
        %td= counts[:sdb_last_hour]
        %td= counts[:mc_this_hour]
        %td= counts[:mc_last_hour]

- content_for :page_javascript do
  :plain
    $(function() {
      $.tablesorter.addParser({
        id: 'name-column',
        is: function(s) { return false; },
        type: 'text',
        format: function(s) {
          return s.replace(/-/g, '_');
        }
      });
      $("#fails").tablesorter({
        widgets: ['zebra'],
        headers: {
          0: { sorter: 'name-column' }
        },
        sortList: [ [1, 1], [2, 1] ]
      });
    });
  
- content_for :page_styles do
  :plain
    table.tablesorter {
      border-collapse: separate;
    }

    table.tablesorter thead tr .header {
      background-position: right bottom;
    }
    
    td.bad_domain {
      border: 2px solid red;
    }
