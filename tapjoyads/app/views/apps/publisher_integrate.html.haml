- @page_title = 'Publisher Integration'
- @dropdown_options = { :with_new_app_button => true }

%h3 Publisher Integration
%ol
  %li
    Select our
    - sdk_link 'Offers Library', "#{@app.platform}-pub"
    or
    = succeed '.' do
      - sdk_link 'Virtual Goods Library', "#{@app.platform}-vg"
  %li Add the Tapjoy library to your project directory.
  %li Compile and run your project.
  %li Add the Tapjoy credentials (provided below) to your app.
  %li
    Email
    = mail_to('support+enableapp@tapjoy.com')
    for confirmation and to make your campaign live.

%p
  For more detailed documentation, please read the documentation included in the library.

%p
  Need help? Please contact our #{mail_to('support@tapjoy.com', 'support team')}.

%div
  %strong Tapjoy App ID:
  .app_id= @app.id
  = clippy(@app.id)
%div
  %strong App Secret Key:
  .app_id= @app.secret_key
  = clippy(@app.secret_key)

%p
  .left.download_link
    - sdk_link 'Offers Library Download', "#{@app.platform}-pub"
  .clear
  .left.download_link
    - sdk_link 'Virtual Goods Library Download', "#{@app.platform}-vg"
  .clear

- content_for :page_head, stylesheet_link_tag('facebox')
- content_for :page_styles do
  :plain
    .download_link {
      margin-top: 10px;
      font-weight: bold;
      font-size: 120%;
    }

    .app_id {
      display: inline;
      font-family: courier;
    }
