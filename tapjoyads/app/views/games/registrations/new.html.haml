- @page_title = 'Register'

%h1 Register

- form_for([ :games, @gamer ], :url => { :action => :create }) do |f|
  .field
    = f.label :email
    = f.text_field :email
    = error_message_on @gamer, :email
  .field
    = f.label :password
    = f.password_field :password
    = error_message_on @gamer, :password
  .field
    = f.hidden_field :referrer, :value => params[:referrer]
    = f.submit 'Register'
