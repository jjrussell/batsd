%p Push notifications can be used to notify your users when offers are completed and bring them back into your app.
.button-container
  - if @app
    %button Enable Push notifications for this app
  - else 
    %button Disable Push notifications for this App
- if @admin_url
  .frame-container
    %iframe{:src => @admin_url, :style => "width: 100%; height: 650px; border: 0px;"}

- content_for :page_styles do
  :plain
    .button-container {
      text-align: center;
    }
    
    .frame-container iframe {
      width: 100%; 
      height: 650px; 
      border: 0px;
    }