- @offer.banner_creative_sizes_with_labels.each do |size_hash|
  %iframe.creative_upload{:src => offer_creative_form_path(:offer_creative_id => @offer.id, :image_size => size_hash[:size], :label => size_hash[:label])}
