- @page_title = "Earn #{@publisher_app.name} #{@currency.name}"

#instructions
  Important instructions: Install and OPEN one of the applications below to receive #{@currency.name}.
  You will receive your #{@currency.name} within a few minutes after 
  %i opening
  the downloaded application.
  
%ul#offers
  - @offer_list.each do |offer|
    %li
      %a{ :href => offer.get_redirect_url(@publisher_app, @publisher_user_record, params[:udid], params[:app_version]) }
        %img{ :src => offer.get_icon_url }
        .offertext
          %span.offername #{offer.name} (#{offer.cost})
          %br
          Earn
          %span.amount #{@currency.get_reward_amount(offer)} #{@currency.name}
          
-content_for :page_styles do
  :plain
    * {
      margin: 0;
      padding: 0;
      border: 0;
      outline: 0;
      font-weight: inherit;
      font-style: inherit;
      font-size: inherit;
      font-family: inherit;
      vertical-align: baseline;
    }

    body {
      font-family: Helvetica, "Tahoma Bold", Arial, sans-serif;
    }

    #instructions { 
      padding: 10px;
      margin: 10px 3%;
      color: #004E09;
      font-weight: bold;
      font-size: 75%;
      text-align: center;
      background: white;
      border: 1px solid #d5d5d5;
      -moz-border-radius: 7px;
      -webkit-border-radius: 7px;
    }

    #offers {
      margin: 0 3%;
      list-style: none;
    }

    #offers a {
      display: block;
      padding: 5px;
      text-decoration: none;
      font-size: 90%;
      color: #004E09;
      font-weight: bold;
      height: 65px;
    }

    #offers li {
      border: 1px solid #d5d5d5;
      border-top: none;
      background: white url(../images/arrowicon.png) no-repeat right;
    }

    #offers li a img {
      padding-top: 5px;
      width: 57px;
      height: 57px;
      float: left;
    }

    #offers li a .offertext {
      height: 65px;
      display: table-cell;
      vertical-align: middle;
      padding-left: 10px;
    }

    #offers .amount {
      color: rgb(0, 136, 16);
    }

    #offers li:first-child {
      border-top:1px solid #d5d5d5;
      -moz-border-radius-topleft:10px;
      -webkit-border-top-left-radius:10px;
      -moz-border-radius-topright:10px;
      -webkit-border-top-right-radius:10px;
    }

    #offers li:last-child {
      -moz-border-radius-bottomleft:10px;
      -webkit-border-bottom-left-radius:10px;
      -moz-border-radius-bottomright:10px;
      -webkit-border-bottom-right-radius:10px;
    }
    
